<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>

  <body>
    <div class="container" style="margin-top:2rem;">
      <div class="row my-5">
        <div class="col-md-6">
          <%= link_to "New Doctor", new_admin_url, class: "submit-btn" %>
        </div>
      </div>

      <h1>Listing Articles</h1>

      <table class="table table-hover">
        <tr>
          <th>Name</th>
          <th>Speciality</th>
          <th>Contact Number</th>
          <th>Email</th>
          <th colspan="3"></th>
        </tr>

        <% @doctors.each do |doc| %>
          <tr>
            <td><%= doc.name %></td>
            <td><%= doc.category %></td>
            <td><%= doc.phone_no %></td>
            <td><%= doc.email %></td>
            <td><%= link_to 'Show', admin_url(doc) %></td>
            <td><%= link_to 'Edit', edit_admin_url(doc) %></td>
            <td><%= link_to 'Destroy', admin_url(doc), method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
        <% end %>
      </table>
    </div>
  </body>
</html>
